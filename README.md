# plataforma_mi_plan

Terminal 1  (backend)              Terminal 2  (frontend)
cd backend                         cd frontend
source .venv/bin/activate   ←      (el .venv NO se necesita aquí)
python manage.py runserver         npm run dev
